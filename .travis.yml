language: python
python: 3.5
addons:
  apt:
    packages:
    - libboost-dev
    - libhdf5-dev
    - python3-all-dev
install:
  - wget -q http://mirrors.kernel.org/ubuntu/pool/universe/p/pybind11/pybind11-dev_2.0.1-4_all.deb
  - sudo dpkg -x *.deb /
script:
  - cmake src
  - make -j4 -l4
sudo: true
